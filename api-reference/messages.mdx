---
title: "POST /v1/messages"
description: "Claude Messages 兼容接口。"
---

## 用途

用于兼容 Claude Messages 请求格式。

## 请求

- Method: `POST`
- URL: `https://alltoken.aatest.online/v1/messages`
- Auth: `Authorization: Bearer YOUR_API_KEY`
- Content-Type: `application/json`

## 最小请求体

```json
{
  "model": "claude-3-5-sonnet",
  "max_tokens": 512,
  "messages": [
    {
      "role": "user",
      "content": "请介绍 AllTokens。"
    }
  ]
}
```

## 请求示例

```bash
curl https://alltoken.aatest.online/v1/messages \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -d '{
    "model": "claude-3-5-sonnet",
    "max_tokens": 512,
    "messages": [
      {
        "role": "user",
        "content": "请介绍 AllTokens。"
      }
    ]
  }'
```

## 返回

成功时返回 Claude Messages 兼容结构。

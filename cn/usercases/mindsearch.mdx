---
title: "在 MindSearch 中使用"
description: "将 Passto AllTokens 配置为 MindSearch 的模型接口来源。"
---

## 概述

MindSearch 在执行搜索、推理和总结时，需要一个可用的模型接口。

将 Passto AllTokens 作为统一模型入口后，你可以在 MindSearch 中复用同一套模型与鉴权配置。

## 配置思路

如果你的 MindSearch 支持 OpenAI 兼容方式接入，通常只需要配置：

- API Key：`YOUR_API_KEY`
- Base URL：`https://alltoken.aatest.online/v1`
- Model：例如 `glm-4`

## 推荐步骤

1. 打开 MindSearch 的模型配置或环境变量配置位置。
2. 将基础地址设置为 `https://alltoken.aatest.online/v1`。
3. 填入 Passto AllTokens API Key。
4. 指定一个已开通的模型名称。
5. 执行一次搜索或总结任务验证返回结果。

## 常见问题

### 调用超时或无响应

先使用更稳定的基础模型验证接入链路，再切换到目标模型。

### 返回 404

请检查配置的路径是否是标准 OpenAI 兼容入口，而不是站点首页地址。

## 下一步

- 获取 API Key：参见 [快速上手](/cn/quickstart)
- 查看接口文档：参见 [接口参考](/cn/api-reference/introduction)
